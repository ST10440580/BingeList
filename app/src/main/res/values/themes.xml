<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme -->
    <style name="Theme.BingeList.NoActionBar" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Brand Colors -->
        <item name="colorPrimary">@color/binge_red</item>
        <item name="colorPrimaryVariant">#B00710</item>
        <item name="colorOnPrimary">@color/white</item>

        <!-- Accent Colors -->
        <item name="colorSecondary">@color/binge_red</item>
        <item name="colorSecondaryVariant">#B00710</item>
        <item name="colorOnSecondary">@color/white</item>

        <!-- Dark Background & Status Bar -->
        <item name="android:windowBackground">@color/binge_surface</item>
        <item name="android:statusBarColor">@color/binge_surface</item>
    </style>
</resources>